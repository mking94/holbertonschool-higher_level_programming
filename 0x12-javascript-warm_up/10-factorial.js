#!/usr/bin/node
const n = process.argv[2];
function factorial (n) {
  if (isNaN(n) || x === 1) {
    return (1);
  } else {
    return (n * factorial(n - 1));
  }
}
console.log(factorial(Number(x)));

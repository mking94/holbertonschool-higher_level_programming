#!/usr/bin/node
const args = process.argv;
const arglen = args.length;
if (arglen === 2) {
  console.log('No argument');
} else if (arglen === 3) {
  console.log('Arguments found');
} else {
  console.log('Arguments found');
}
